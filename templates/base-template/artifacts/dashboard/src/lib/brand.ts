// ─────────────────────────────────────────────────────────────────────────────
// BRANDING — edit this one file (plus the theme block in src/index.css and the
// logo at public/logo.webp) to rebrand the whole app for a company / product.
//
// When adapting this template for a specific company, set these to match their
// product name and identity. See CLAUDE.md → "Apply branding".
// ─────────────────────────────────────────────────────────────────────────────
export const BRAND = {
  /** Sidebar title / product name shown in the header (e.g. "Acme Console"). */
  productName: "AgenticOS",
  /** Small sidebar subtitle under the product name. */
  productTagline: "Template",
  /** Company the OS is built for — used in copy and placeholder data. */
  companyName: "Your Company",
  /** Logo shown in the sidebar + favicon. Drop a new file at public/logo.webp. */
  logoSrc: "/logo.webp",
  /** Platform attribution shown in the sidebar footer. */
  poweredBy: "Powered by Lyzr AgenticOS",
};
